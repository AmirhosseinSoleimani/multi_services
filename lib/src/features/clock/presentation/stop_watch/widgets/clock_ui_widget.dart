import 'package:flutter/material.dart';

class ClockUiWidget extends StatelessWidget {
  const ClockUiWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
